# Legacy OpenVLA-OFT launchers

These shell and comparison scripts are preserved unchanged from the former
combined OpenVLA-OFT release branch.

They reference intermediate runners or configs that were removed during the
earlier reproducibility cleanup. They are retained so no public research
artifact is lost, but they are not supported release entry points. Use the
three configs and runners documented in the repository root `README.md`.
